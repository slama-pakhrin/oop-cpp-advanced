#define _CRT_SECURE_NO_WARNINGS_

#include "AirportModels.h"
using namespace std;

namespace sdds
{
    std::ostream& operator<<(std::ostream& obj, const Airport& airport) 
    {
    if(obj.name == '0' )
    {

    }
    }
}


//std::ostream& operator<<(std::ostream& os, const Airport& airport) {
    //os << "Airport Name: " << airport.getName() << ", Latitude: " << airport.getLatitude();
   // return os;